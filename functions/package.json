{
  "name": "scout-planner-functions",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "npx functions-framework --target=clone-timetable",
    "test": "vitest --globals",
    "lint": "eslint --no-error-on-unmatched-pattern **/*.js **/*.ts",
    "format": "prettier --check ."
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "eslint": "^9.22.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3",
    "vitest": "^3.0.9"
  }
}
